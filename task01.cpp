#include<iostream>
using namespace std;

main()
{
     
     int arr[5];
     int sum;
    for(idx=0 ; idx<=5 ; idx++)
    {
        cout << "enter number:";
        cin >> arr[idx];
        sum=sum+arr[idx];
    }
}